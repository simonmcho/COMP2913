import React, { Component } from 'react';

// Displays same request state
class SameRequest extends Component { 
    render() {
        return (
            <div style={{ textAlign: 'center' }}>
                <span style={{ color: '#cc0000' }}>SAME REQUEST MADE!</span>
            </div>
        )
    }
}

export default SameRequest;